# ─────────────────────────────────────────────────────────────────────────────
# Core dependencies — install everything with:
#   pip install -r requirements.txt
#   playwright install chromium
# ─────────────────────────────────────────────────────────────────────────────

# Browser automation (rendering engine)
playwright>=1.62.0

# Hugging Face — dataset streaming + hub upload
datasets>=3.0.0
huggingface_hub>=0.24.0

# Config & env
pyyaml>=6.0
python-dotenv>=1.0.0

# Data loading (fonts manifest Excel file)
pandas>=2.0.0
openpyxl>=3.1.0

# Progress bar
tqdm>=4.60.0

# Asyncio compatibility (fallback for some environments)
nest_asyncio>=1.6.0

# Data augmentation (document-realistic image transforms)
augraphy>=8.0.0
opencv-python-headless>=4.8.0
numpy>=1.24.0
arabic-reshaper>=3.0.0
python-bidi>=0.4.2
